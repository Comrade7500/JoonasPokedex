<!DOCTYPE html>
<html lang="fi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokemon-sukupolvet</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/styles/styles.css" rel="stylesheet">
</head>
<body>
    <%- include('header') %>
    <div class="container">
        <h1 class="my-4">Pokemon sukupolvet</h1>
        

        <form action="/pokemon" method="GET" class="mb-4">
            <div class="input-group">
                <input type="text" name="nimi" class="form-control" placeholder="Syötä Pokemonin nimi" required>
                <button type="submit" class="btn btn-primary">Hae</button>
            </div>
        </form>

        <div class="d-grid gap-2">
            <% for (let i = 1; i <= 9; i++) { %>
                <a href="/sukupolvi/<%= i %>" class="btn btn-primary btn-lg">Sukupolvi <%= i %></a>
            <% } %>
        </div>
    </div>
    <%- include('footer') %>
</body>
</html>
